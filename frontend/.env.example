# Base URL of the Canvex backend. The WebSocket URL is derived from this by
# swapping http→ws / https→wss. In production (Vercel), set this to your
# Render/Railway backend URL, e.g. https://canvex-api.onrender.com
VITE_API_URL=http://localhost:8000
